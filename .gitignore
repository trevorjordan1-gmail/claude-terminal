# Audit output snapshots — contain hostnames, SSH aliases, project names.
# Never commit these.
os-audit*/
*.tar.gz

# Maintainer's local agent context — references private infrastructure.
CLAUDE.md

# Editor / OS noise
*.swp
.DS_Store
